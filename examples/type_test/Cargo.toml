[package]
name = "type_test"
version = "1.2.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[features]
default = []
from_save = []

[dependencies]
cc_wasm_api = { path = "../../../cc_wasm_api", default-features = false, features = [
    "coroutine",
    "eval",
    "debug",
] }
